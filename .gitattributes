# Set text files to be converted to CRLF line endings
* text=auto

# Explicitly define files that should use LF line endings
*.md text eol=lf

# Explicitly define files that should use CRLF line endings
*.txt text eol=crlf
